import {createServerSupabase,hasSupabaseConfig} from '@/lib/supabase-server';
const agents=['Context / evidence retrieval','Physics / deterministic baseline','Measurement / CV extraction','Calibration / system identification','Residual ML / validation','Uncertainty / risk gate','Experiment / next selection','Critic / engineering honesty'];
export default async function Graph(){if(!hasSupabaseConfig())return <main className="page"><h1 className="title">Sign in required</h1></main>;try{const s=await createServerSupabase();const {data:{user}}=await s.auth.getUser();if(!user)return <main className="page"><h1 className="title">Sign in required</h1></main>;return <main className="page"><div className="eyebrow">AGENT GRAPH / ENGINEERING ONLY</div><h1 className="title">Evidence → prediction → measurement → learning</h1><div className="grid grid2" style={{marginTop:24}}><div className="panel"><h2>EXECUTION GRAPH</h2>{['Engineering input','Structured problem','Physics simulation','Deterministic checks','Uncertainty/domain randomization','Real inspection measurement','System identification','Residual ML','Validated uncertainty','Next experiment'].map((x,i)=><div className="step" key={x}><div><b>{String(i+1).padStart(2,'0')} / {x}</b><br/><small>{i<4?'deterministic':'requires real evidence'}</small></div></div>)}</div><div className="panel"><h2>AGENTS</h2>{agents.map(x=><div className="step" key={x}><div><b>{x}</b><br/><small>structured state only · no autonomous financial/commercial role</small></div></div>)}</div></div></main>}catch{return <main className="page"><h1 className="title">Sign in required</h1></main>}}