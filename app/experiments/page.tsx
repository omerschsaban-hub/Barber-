'use client';import {useState} from 'react';
export default function Experiments(){const [result,setResult]=useState<any>();async function choose(){const r=await fetch(`${process.env.NEXT_PUBLIC_ENGINEERING_URL||'http://localhost:8000'}/v1/next-experiment?machine_id=unassigned`,{method:'POST'});setResult(await r.json())}return <main className="page"><div className="eyebrow">ACTIVE LEARNING</div><h1 className="title">Choose the next physical experiment.</h1><p className="muted">The experiment engine is intentionally conservative: with no real residual history, it asks for a baseline measurement instead of pretending it knows which parameter matters.</p><div className="panel" style={{marginTop:24}}><button className="button" onClick={choose}>Select next experiment</button>{result&&<div style={{marginTop:22}}><span className="status good">{result.experiment.expected_information_gain} information value</span><h2>{result.experiment.type}</h2><p>Print: {result.experiment.dimensions_mm.join(' × ')} mm</p><p className="muted">Features: {result.experiment.features.join(', ')}</p><p className="annotation">{result.experiment.reason}</p></div>}</div></main>}