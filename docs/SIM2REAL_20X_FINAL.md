## Scope locked

The target is the existing 20 capabilities, sharpened by a shared CV + deterministic + physics + ML + evidence architecture.

No product sprawl. No fabricated data. No LLM-generated engineering truth.

The branch is ready for repository CI and deployed end-to-end validation.