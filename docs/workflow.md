# Fabrient workflow

The physical-product workflow is:

**Design → Build → Test → Verify → Manufacture/Release**

## Build

Build is a first-class stage after Design. It turns an approved design into a buildable/fabrication-ready physical artifact or build package. Build has its own status and must complete before Test can begin.
