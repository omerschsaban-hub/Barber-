'use client';
import {useEffect} from 'react';
import {loadLocalLoop,saveLoop} from '@/lib/engineering-loop';
export default function EngineeringLoopTracker(){useEffect(()=>{let active=true;const original=window.fetch.bind(window);const track=async(input:RequestInfo|URL,init?:RequestInit)=>{const response=await original(input,init);try{const url=typeof input==='string'?input:input instanceof URL?input.toString():input.url;if(!url.includes('/v1/'))return response;const clone=response.clone();const data=await clone.json().catch(()=>null);if(!active||!data)return response;const previous=loadLocalLoop();let next:any=null;if(url.includes('/v1/predict')&&response.ok)next={stage:'review',status:'ready',next_action:'Review the verified prediction and decide whether to move to build.',last_action:'Completed deterministic prediction',evidence_summary:{prediction:data.prediction_mm,interval:data.interval_95_mm}};else if(url.includes('/v1/import/preview')&&response.ok)next={stage:'inspect',status:'ready',next_action:'Confirm the inspection mapping and ingest real measurements.',last_action:'Previewed inspection record',evidence_summary:{import_rows:data.row_count_preview,sha256:data.content_sha256}};else if(url.includes('/v1/reverification')&&response.ok)next={stage:'reverify',status:data.interval_days?'ready':'blocked',next_action:data.interval_days?'Run the next physical verification when the interval is reached.':'Do not release: no defensible re-verification interval exists.',last_action:'Calculated re-verification interval',evidence_summary:{reverification:data}};if(next){const merged={...previous,...next};saveLoop(merged);window.dispatchEvent(new CustomEvent('fabrient:loop-updated',{detail:merged}))}}catch{}return response};window.fetch=track as typeof window.fetch;return()=>{active=false;window.fetch=original}},[]);return null}
