name: Engineering smoke tests

on:
  push:
    branches: [main]
    paths:
      - 'engineering/**'
      - 'services/mcp/**'
      - 'mcp_capabilities.py'
      - 'Dockerfile'
      - 'render.yaml'
      - '.github/workflows/engineering-smoke.yml'
  pull_request:
    paths:
      - 'engineering/**'
      - 'services/mcp/**'
      - 'mcp_capabilities.py'
      - 'Dockerfile'
      - 'render.yaml'
      - '.github/workflows/engineering-smoke.yml'

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: pip install -r engineering/requirements.txt -r requirements.txt
      - name: Run engineering and MCP surface tests
        run: PYTHONPATH=engineering:. pytest -q engineering/tests/test_mcp_tool_surface.py
