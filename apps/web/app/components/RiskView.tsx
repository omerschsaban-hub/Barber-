'use client'
import {useEffect,useRef} from 'react'
import * as THREE from 'three'
export default function RiskView({size=[40,20,10],risk=0}:{size?:number[];risk?:number}){const ref=useRef<HTMLDivElement>(null);useEffect(()=>{if(!ref.current)return;const el=ref.current;const scene=new THREE.Scene();scene.background=new THREE.Color(0x090908);const camera=new THREE.PerspectiveCamera(45,1,.1,1000);camera.position.set(55,45,55);camera.lookAt(0,0,0);const renderer=new THREE.WebGLRenderer({antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.setSize(el.clientWidth,320);el.appendChild(renderer.domElement);const geom=new THREE.BoxGeometry(size[0],size[1],size[2]);const mat=new THREE.MeshStandardMaterial({color:risk>=1?0xb53a32:risk>=.5?0xd9b33b:0x7aa66d,roughness:.75});const mesh=new THREE.Mesh(geom,mat);scene.add(mesh);const edges=new THREE.LineSegments(new THREE.EdgesGeometry(geom),new THREE.LineBasicMaterial({color:0xe9e7df}));scene.add(edges);scene.add(new THREE.AmbientLight(0xffffff,.7));const light=new THREE.DirectionalLight(0xffffff,1.1);light.position.set(30,40,50);scene.add(light);let raf=0;const loop=()=>{mesh.rotation.y+=.003;edges.rotation.y=mesh.rotation.y;renderer.render(scene,camera);raf=requestAnimationFrame(loop)};loop();const ro=new ResizeObserver(()=>{if(!ref.current)return;renderer.setSize(ref.current.clientWidth,320);camera.aspect=ref.current.clientWidth/320;camera.updateProjectionMatrix()});ro.observe(el);return()=>{cancelAnimationFrame(raf);ro.disconnect();renderer.dispose();geom.dispose();mat.dispose();el.removeChild(renderer.domElement)}},[size.join(','),risk]);return <div ref={ref} aria-label="Computed 3D engineering risk visualization" style={{width:'100%',minHeight:320}}/>}
