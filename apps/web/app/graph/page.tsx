import Link from 'next/link'

export default function Graph(){const nodes=['Context / evidence','Physics baseline','Deterministic validation','Real measurement / CV','System identification','Residual ML','Uncertainty gate','Next experiment'];return <main><header><strong>FABRIENT / ENGINEERING GRAPH</strong><Link href='/'>Home</Link></header><section><div className='eyebrow'>TRACEABLE EXECUTION</div><h1>Sim-to-real graph</h1><p className='lede'>Each stage consumes explicit evidence and produces traceable state. No autonomous stage may invent engineering measurements.</p>{nodes.map((n,i)=><article key={n} style={{margin:'10px 0',borderLeft:'3px solid #d9b33b'}}><b>{String(i+1).padStart(2,'0')} — {n}</b><p>{i===0?'Source records, structured inputs and provenance':i===1?'Deterministic process model and stated assumptions':i===2?'Units, tolerance and range gates':i===3?'Measured ground truth; CV requires physical scale':i===4?'Machine/process-specific effects from observations':i===5?'Residual only after sufficient real data':i===6?'Combined uncertainty and validation state':'Select experiment targeting largest reducible uncertainty'}</p></article>)}</section></main>}