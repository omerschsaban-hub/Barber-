'use client'
import Link from 'next/link'
import {useState} from 'react'
export default function ImportPage(){const[text,setText]=useState('');const[rows,setRows]=useState<string[][]>([]);function parse(){const lines=text.split(/\r?\n/).filter(Boolean);const parsed=lines.map(l=>l.split(',').map(x=>x.trim()));setRows(parsed)}return <main><header><strong>FABRIENT / RECORD IMPORT</strong><Link href='/'>Home</Link></header><section><div className='eyebrow'>EXISTING RECORDS FIRST</div><h1>Import inspection records</h1><p className='lede'>Paste messy CSV-like records. Fabrient previews the mapping before any measurement is treated as engineering evidence.</p><textarea value={text} onChange={e=>setText(e.target.value)} placeholder='serial,feature,nominal_mm,measured_mm,tolerance_mm'/><br/><button onClick={parse}>Preview mapping</button>{rows.length>0&&<article><h2>Unconfirmed preview</h2><table><tbody>{rows.map((r,i)=><tr key={i}>{r.map((c,j)=><td key={j} style={{padding:8,border:'1px solid #333'}}>{c}</td>)}</tr>)}</tbody></table><p>Nothing has been imported as ground truth yet. Confirmed rows must carry source provenance and units.</p></article>}</section></main>}