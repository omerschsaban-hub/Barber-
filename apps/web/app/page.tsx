'use client'
import {useEffect,useState} from 'react'
import {supabase} from '../lib/supabase'
export default function Page(){const[s,setS]=useState<any>(null);const[name,setName]=useState('');const[msg,setMsg]=useState('');useEffect(()=>{supabase.auth.getSession().then(({data})=>setS(data.session));const x=supabase.auth.onAuthStateChange((_e,session)=>setS(session));return()=>x.data.subscription.unsubscribe()},[]);async function google(){await supabase.auth.signInWithOAuth({provider:'google',options:{redirectTo:location.origin}})}async function create(){if(!s||!name)return;const{data,error}=await supabase.from('projects').insert({name,owner_id:s.user.id}).select().single();setMsg(error?.message??`Created ${data.name}`)}return <main><header><strong>FABRIENT</strong>{s?<button onClick={()=>supabase.auth.signOut()}>Sign out</button>:<button onClick={google}>Continue with Google</button>}</header><section><div className='eyebrow'>SIM → REAL / FDM</div><h1>Dimensional engineering, grounded in the physical world.</h1><p className='lede'>Physics baseline → deterministic engineering checks → uncertainty/domain variation → existing inspection records → real measurements → machine-specific system identification → residual ML → calibrated uncertainty → next experiment.</p>{s?<div className='grid'><article><h2>Project</h2><input placeholder='Project name' value={name} onChange={e=>setName(e.target.value)}/><button onClick={create}>Create project</button><p>{msg}</p></article><article><h2>Engineering gates</h2><ul><li>Measurements are ground truth.</li><li>Calibration requires real observations.</li><li>Every prediction carries provenance and uncertainty.</li><li>CV cannot infer millimetres without a physical reference.</li><li>Unsupported tolerances are refused.</li><li>Production drift and service wear remain separate.</li></ul></article></div>:<article><h2>Google sign-in</h2><p>Fabrient uses Google authentication only. No passwords are stored by the application.</p><button onClick={google}>Sign in with Google</button></article>}</section></main>}