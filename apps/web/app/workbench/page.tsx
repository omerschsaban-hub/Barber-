'use client'
import {useState} from 'react'
export default function Workbench(){const[x,setX]=useState({nominal_mm:40,material:'PETG',machine:'',shrinkage_mean:.003,shrinkage_std:.0015,layer_height_mm:.2});const[p,setP]=useState<any>(null);const[e,setE]=useState('');async function run(){setE('');try{const r=await fetch((process.env.NEXT_PUBLIC_ENGINE_URL||'http://localhost:8000')+'/physics/predict',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(x)});const j=await r.json();if(!r.ok)throw Error(j.detail||'Engineering service error');setP(j)}catch(err:any){setE(err.message)}}return <main><header><strong>FABRIENT / WORKBENCH</strong><a href='/'>Home</a></header><section><div className='eyebrow'>DETERMINISTIC BASELINE</div><h1>Engineering input</h1><p className='lede'>Natural language can assist with parsing, but these fields are the authoritative engineering inputs.</p><div className='grid'><article>{Object.entries(x).map(([k,v])=><label key={k}>{k}<input value={String(v)} onChange={ev=>setX({...x,[k]:['material','machine'].includes(k)?ev.target.value:Number(ev.target.value)})}/></label>)}<button onClick={run}>Run physics baseline</button></article><article><h2>Prediction</h2>{e&&<p>{e}</p>}{p?<pre>{JSON.stringify(p,null,2)}</pre>:<p>No prediction yet.</p>}<p>State is <b>{p?.status||'not_calculated'}</b>. No machine calibration is assumed.</p></article></div></section></main>}